# fel-drone
node_modules

dist
generated
.env
.env.*
!.env.example
*.local
.DS_Store
image-search/
tsconfig.tsbuildinfo

# local dev API store (never committed)
.dev-data

# transportable git bundles (session hand-off artifacts, never committed)
*.bundle
